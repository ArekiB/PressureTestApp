<Window x:Class="PressureTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Система испытаний давления" Height="750" Width="950"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Система испытаний давления" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBox x:Name="TestNameBox" Width="200" Padding="5" Margin="0,0,10,0"/>
            <Button x:Name="StartBtn" Content="Старт" Width="100" Height="35" Margin="0,0,10,0" IsEnabled="False"/>
            <Button x:Name="StopBtn" Content="Стоп" Width="100" Height="35" Margin="0,0,10,0" IsEnabled="False"/>
            <Button x:Name="SettingsBtn" Content="Настройки" Width="100" Height="35" Margin="0,0,10,0"/>
            <Button x:Name="HistoryBtn" Content="История" Width="100" Height="35" Margin="0,0,10,0"/>
        </StackPanel>

        <!-- График -->
        <lvc:CartesianChart x:Name="PressureChart" Grid.Row="2" Margin="0,10"/>

        <!-- Статистика и индикация -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Индикатор связи Modbus -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                <Ellipse x:Name="ConnectionIndicator" Width="12" Height="12" Fill="Red" Margin="0,0,5,0"/>
                <TextBlock x:Name="ConnectionStatusText" Text="Disconnected" Foreground="Red"/>
            </StackPanel>

            <!-- Статистика -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Макс:" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock x:Name="MaxValueText" Width="60" Foreground="Green"/>
                <TextBlock Text="Мин:" FontWeight="Bold" Margin="15,0,5,0"/>
                <TextBlock x:Name="MinValueText" Width="60" Foreground="Red"/>
                <TextBlock Text="Сред:" FontWeight="Bold" Margin="15,0,5,0"/>
                <TextBlock x:Name="AvgValueText" Width="60"/>
            </StackPanel>

            <!-- Текущее давление -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <TextBlock Text="Текущее давление:" FontWeight="Bold" Margin="0,0,10,0"/>
                <TextBlock x:Name="CurrentPressureText" FontWeight="Bold" Foreground="Blue" Width="80"/>
                <TextBlock Text="усл. ед."/>
            </StackPanel>
        </Grid>

        <!-- Статус -->
        <TextBlock x:Name="StatusText" Grid.Row="4" Foreground="Gray" Margin="0,10,0,0"/>

        <!-- Отладка -->
        <Border Grid.Row="5" Background="#F0F0F0" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="0,10,0,0" Padding="5">
            <ScrollViewer Height="80" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="DebugText" Foreground="Blue" FontSize="10" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>