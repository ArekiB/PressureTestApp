<Window x:Class="PressureTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Система испытаний давления" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Система испытаний давления" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button x:Name="BtnSettings" Content="Настройки" Width="120" Height="35" 
                    Click="BtnSettings_Click" Margin="0,0,10,0"/>
            <Button x:Name="BtnStart" Content="Старт" Width="120" Height="35" 
                    IsEnabled="False" Margin="0,0,10,0"/>
            <Button x:Name="BtnStop" Content="Стоп" Width="120" Height="35" 
                    IsEnabled="False"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="1" 
                Background="White" CornerRadius="5">
            <TextBlock Text="График давления будет отображаться здесь после запуска испытания" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Foreground="Gray" TextWrapping="Wrap" TextAlignment="Center"/>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,20,0,0">
            <TextBlock Text="Статус:" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBlock x:Name="TxtStatus" Text="Готов к работе" Foreground="Green"/>
        </StackPanel>
    </Grid>
</Window>