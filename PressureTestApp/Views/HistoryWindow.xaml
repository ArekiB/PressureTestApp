<Window x:Class="PressureTestApp.Views.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="История испытаний" Height="500" Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="История испытаний" FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="FilterBox" Padding="5" Tag="Поиск по названию..."/>

            <TextBlock Grid.Column="1" Text="Сортировать:" VerticalAlignment="Center" Margin="10,0,5,0"/>
            <ComboBox x:Name="SortCombo" Grid.Column="2" Width="180" SelectedIndex="0">
                <ComboBoxItem Content="По дате (новые сверху)"/>
                <ComboBoxItem Content="По дате (старые сверху)"/>
                <ComboBoxItem Content="По названию (А-Я)"/>
                <ComboBoxItem Content="По названию (Я-А)"/>
            </ComboBox>
        </Grid>

        <DataGrid x:Name="SessionsGrid" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding DisplayName}" Width="200"/>
                <DataGridTextColumn Header="Дата и время начала" Binding="{Binding StartTime, StringFormat=dd.MM.yyyy HH:mm:ss}" Width="150"/>
                <DataGridTextColumn Header="Дата и время окончания" Binding="{Binding EndTime, StringFormat=dd.MM.yyyy HH:mm:ss}" Width="150"/>
                <DataGridTextColumn Header="Тип эмуляции" Binding="{Binding EmulationType}" Width="100"/>
                <DataGridTextColumn Header="Параметр" Binding="{Binding Param1, StringFormat=F3}" Width="80"/>
                <DataGridTextColumn Header="Точек" Binding="{Binding PointsCount}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="LoadBtn" Content="Загрузить на график" Width="140" Height="30" Margin="0,0,10,0" IsEnabled="False"/>
            <Button x:Name="DeleteBtn" Content="Удалить" Width="80" Height="30" Margin="0,0,10,0" IsEnabled="False"/>
            <Button x:Name="CloseBtn" Content="Закрыть" Width="80" Height="30"/>
        </StackPanel>
    </Grid>
</Window>